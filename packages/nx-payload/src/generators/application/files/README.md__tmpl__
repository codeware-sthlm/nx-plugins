# <%= name %>

Payload admin application generated by Nx plugin `@codeware-sthlm/nx-payload`.

## Getting started

The application can be started in Docker with a local Mongo database instance.

```sh
docker compose -f <%= directory %>/docker-compose.yml up -d
```

### Admin page

<http://localhost:3000/admin>

### Mongo connection

`mongodb://mongo/<%= name %>`

### Stop and remove Docker containers

```sh
docker compose -f <%= directory %>/docker-compose.yml down
```
